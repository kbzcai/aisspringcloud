spring:
  cloud:
    config:
      name: configclient
      discovery:
        enabled: true
        service-id: configserver
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/